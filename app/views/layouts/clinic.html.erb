<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "mCard - Clinical Data Protal" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fixed-top navbar-choc">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/clinic/home">Clinical Data</a>
        </div>  

        <div class="collapse navbar-collapse" id="myMenu">
          <% if !current_user %>
            <ul class="nav navbar-nav">
            </ul>
          <% else %>
            <%= render :partial => "layouts/clinic_menubar" %>
          <% end %>
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <div class="container-fluid">
      <!-- error-message box -->
      <% if flash[:error] %>
        <div class="alert alert-danger">
          <button class="close" data-dismiss="alert">×</button>
          <b>ERROR</b>: <%= flash[:error] %>
      </div>
    <% end %>

    <!-- success-message box -->
    <% if flash[:notice] %>
      <div class="alert alert-success">
        <button class="close" data-dismiss="alert">×</button>
        <i class="icon-ok-sign"> </i> 
        <b>Info</b>: <%= flash[:notice] %>
    </div>
  <% end %>

  <div class="container-fluid">
    <%= yield %>
    <hr>
    <%= link_to 'Back', url_for(:back) %> | &copy;  The Johns Hopkins Schizophrenia Center - <%= Time.now.year %>
  </div>

  </body>
</html>
